apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: likhitha-pvc
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: duniversity-storage   
  resources:
    requests:
      storage: 8Gi